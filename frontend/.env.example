# Base URL of the Express API. The Next.js dev server proxies /api/* to this
# origin (see next.config.ts), so the browser only ever talks to same-origin
# /api paths and this value is only used server-side / at build time.
API_ORIGIN=http://localhost:4000
