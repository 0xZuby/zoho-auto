# Guided Workspace UI Implementation Plan

1. Add focused frontend components for the mascot, step indicator, and auditor sidebar.
2. Replace the onboarding page with a client-side, validation-aware wizard and review state, retaining the existing submission API.
3. Replace the auditor page layout with a sidebar, metrics, search/filterable privacy-safe queue, and full review workspace.
4. Rebuild the global styles around responsive layout, visual hierarchy, accessible controls, and reduced-motion support.
5. Run backend tests, frontend linting, type checking, and production build; correct failures.
